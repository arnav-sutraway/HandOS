# HandOS Web UI

This folder contains the professional presentation layer for HandOS.

## Stack

- React
- TypeScript
- Vite
- Framer Motion

## Run

```powershell
cd web
npm install
npm run dev
```

The web app is intentionally separate from the Python runtime so the product surface and the native gesture engine can evolve independently.
